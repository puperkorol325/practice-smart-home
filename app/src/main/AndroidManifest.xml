<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Practice_house"
        tools:targetApi="31">
        <activity
            android:name=".LoadingActivity"
            android:exported="true"
            android:theme="@style/Theme.Practice_house.Loading">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".AuthActivity"
            android:exported="false"
            android:theme="@style/Theme.Practice_house.Auth" />
        <activity
            android:name=".RegistrationActivity"
            android:exported="false"
            android:theme="@style/Theme.Practice_house.Auth" />
        <activity
            android:name=".CreatePinActivity"
            android:exported="false"
            android:theme="@style/Theme.Practice_house.Auth" />
        <activity
            android:name=".MainActivity"
            android:exported="false"
            android:theme="@style/Theme.Practice_house.Main" />
        <activity
            android:name=".AddRoomActivity"
            android:exported="false"
            android:theme="@style/Theme.Practice_house.Main" />
    </application>

</manifest>