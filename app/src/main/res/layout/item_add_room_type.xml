<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="0dp"
    android:layout_height="wrap_content"
    android:clickable="true"
    android:focusable="true"
    android:gravity="center_horizontal"
    android:orientation="vertical">

    <FrameLayout
        android:id="@+id/add_room_type_icon_container"
        android:layout_width="@dimen/add_room_type_icon_size"
        android:layout_height="@dimen/add_room_type_icon_size"
        android:background="@drawable/bg_add_room_type_circle_default">

        <ImageView
            android:id="@+id/add_room_type_icon"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:contentDescription="@null"
            android:importantForAccessibility="no"
            android:scaleType="fitCenter"
            android:padding="@dimen/add_room_type_icon_inner_padding"
            tools:srcCompat="@drawable/ic_add_room_type_living_default" />

    </FrameLayout>

    <TextView
        android:id="@+id/add_room_type_label"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/add_room_type_label_margin_top"
        android:gravity="center"
        android:textColor="@color/add_room_type_label_default"
        android:textSize="@dimen/add_room_type_label_text_size"
        android:textStyle="bold"
        tools:text="Гостиная" />

</LinearLayout>
